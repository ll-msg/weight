# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0

# ORM 与数据库（SQLite 内置；psycopg 为 PostgreSQL 驱动，Render 等部署时使用）
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3

# 数据校验 / 配置
pydantic==2.10.4
pydantic-settings==2.7.1

# 鉴权（JWT）。密码哈希用标准库 hashlib，无需第三方依赖。
PyJWT==2.10.1
